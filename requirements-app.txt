# Dependencies for the Streamlit app ONLY (app/Home.py and app/pages/).
#
# The app reads the CSV and PNG files the pipeline already wrote; it fits no
# models and recomputes nothing, so it needs none of the analysis stack in
# requirements.txt (scikit-learn, shap, statsmodels, scipy, jupyter, nbconvert).
# That matters for deployment: the full set resolves to ~2.3 GB and will not fit
# a free Streamlit Community Cloud container, while this one is ~490 MB.
#
# Use requirements.txt to run the analysis; use this file to serve the app.
streamlit==1.40.2
pandas==2.2.3
PyYAML==6.0.2
altair==5.4.1
